import { StyleSheet } from "react-native";

export default StyleSheet.create({
  wrapper: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 24,
    marginHorizontal: 24
  },
  title: {
    fontSize: 14,
    fontWeight: '500',
    color: '#A1AFC3',
    flex: 1
  }
});